import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    globals: true,
    environment: 'node',
    root: '.',
    include: ['tests/**/*.test.ts'],
    coverage: {
      provider: 'v8',
      reporter: ['text', 'html'],
      include: ['src/**/*.ts'],
      exclude: ['src/index.ts', 'src/dashboard/public/**'],
    },
    // Ensure config module doesn't throw on missing env vars during tests
    setupFiles: ['tests/setup.ts'],
  },
});
